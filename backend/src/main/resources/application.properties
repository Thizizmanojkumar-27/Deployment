spring.application.name=backend

# Database configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/codenexuss_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Manoj$2005}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port
server.port=${PORT:8080}

# Gemini API configuration
# The key below appears to be a Google Gemini key (AIzaSy...)
gemini.api.key=d5b898c3d069b4f93f4e55271c7c953d
gemini.api.url=https://generativelanguage.googleapis.com/v1/models/gemini-2.0-flash:generateContent

# Admin Portal Authentication
admin.username=admin
admin.password=admin123
